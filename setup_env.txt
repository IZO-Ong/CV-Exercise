1. Install dependencies using pip install -r requirements.txt
2. Add OPENAI_KEY=... in a new .env file and place it inside the folder
3. Open the terminal using this folder's directory and run streamlit run main.py